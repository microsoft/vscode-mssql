-- SET ROWCOUNT
SET ROWCOUNT 10
SET ROWCOUNT @myVar
go

-- multiple options in one set
SET CONCAT_NULL_YIELDS_NULL, CURSOR_CLOSE_ON_COMMIT ON;
SET ARITHABORT, NUMERIC_ROUNDABORT, ANSI_DEFAULTS off
SET STATISTICS IO, TIME, PROFILE on
SET STATISTICS PROFILE, TIME off
go

-- SET OFFSETS
SET OFFSETS SELECT, FROM, ORDER, COMPUTE, TABLE, PROCEDURE, EXECUTE, STATEMENT, PARAM ON
SET OFFSETS FROM, ORDER OFF
SET OFFSETS PARAM ON
