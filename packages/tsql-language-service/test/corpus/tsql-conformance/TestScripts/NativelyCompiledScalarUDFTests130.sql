CREATE FUNCTION [dbo].[Hkmyinc_nullable]
(@a INT NULL)
RETURNS INT
WITH EXECUTE AS SELF, NATIVE_COMPILATION, SCHEMABINDING
AS
BEGIN ATOMIC
WITH (TRANSACTION ISOLATION LEVEL = SNAPSHOT, LANGUAGE = N'us_english')
   RETURN @a + 42;
END;

Go
CREATE FUNCTION [dbo].[Hkmyinc_nullable]
(@a INT NOT NULL)
RETURNS INT
WITH EXECUTE AS SELF, NATIVE_COMPILATION, SCHEMABINDING
AS
BEGIN ATOMIC
WITH (TRANSACTION ISOLATION LEVEL = SNAPSHOT, LANGUAGE = N'us_english')
   RETURN @a + 42;
END;

Go
CREATE FUNCTION [dbo].[Hkmyinc_nullable]
(@a INT NOT NULL)
RETURNS INT
WITH EXECUTE AS SELF, SCHEMABINDING, NATIVE_COMPILATION
AS
BEGIN ATOMIC
WITH (TRANSACTION ISOLATION LEVEL = SNAPSHOT, LANGUAGE = N'us_english')
   RETURN @a + 42;
END;

Go
CREATE FUNCTION [dbo].[Hkmyinc_nullable]
(@a INT NOT NULL)
RETURNS INT
WITH EXECUTE AS SELF, NATIvE_COMPILATIoN, SCHEMABINDING
AS
BEGIN ATOMIC
WITH (TRANSACTION ISOLATION LEVEL = SNAPSHOT, LANGUAGE = N'us_english')
   RETURN @a + 42;
END;