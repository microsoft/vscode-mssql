SELECT AI_ANALYZE_SENTIMENT('text');

SELECT AI_ANALYZE_SENTIMENT(t.text_col)
FROM dbo.Texts AS t;

SELECT AI_ANALYZE_SENTIMENT(text_col)
FROM Texts;

DECLARE @s0 AS NVARCHAR (MAX) = N'Hello world';

SELECT AI_ANALYZE_SENTIMENT(@s0 + 'a');

SELECT AI_ANALYZE_SENTIMENT('b' + 'a');

SELECT *
FROM AI_ANALYZE_SENTIMENT((SELECT t.text_col
                           FROM dbo.Texts AS t));


GO
CREATE OR ALTER FUNCTION dbo.fx
(@s NVARCHAR (MAX))
RETURNS NVARCHAR (MAX)
AS
BEGIN
    RETURN (AI_ANALYZE_SENTIMENT(@s));
END
