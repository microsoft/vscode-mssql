SELECT FIRST_VALUE(Measure) RESPECT NULLS OVER ();

SELECT FIRST_VALUE(Measure) IGNORE NULLS OVER ();

SELECT FIRST_VALUE(Measure) OVER ();

SELECT LAST_VALUE(Measure) RESPECT NULLS OVER ();

SELECT LAST_VALUE(Measure) IGNORE NULLS OVER ();

SELECT LAST_VALUE(Measure) OVER ();

SELECT LAG(Measure) RESPECT NULLS OVER ();

SELECT LAG(Measure) IGNORE NULLS OVER ();

SELECT LAG(Measure) OVER ();

SELECT LEAD(Measure) RESPECT NULLS OVER ();

SELECT LEAD(Measure) IGNORE NULLS OVER ();

SELECT LEAD(Measure) OVER ();
