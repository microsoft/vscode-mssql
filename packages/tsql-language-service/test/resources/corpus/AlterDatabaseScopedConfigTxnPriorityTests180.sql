-- TXN_PRIORITY_MODE = OFF
ALTER DATABASE SCOPED CONFIGURATION SET TXN_PRIORITY_MODE = OFF;

-- TXN_PRIORITY_MODE = ROLLBACK
ALTER DATABASE SCOPED CONFIGURATION SET TXN_PRIORITY_MODE = ROLLBACK;

-- TXN_PRIORITY_MODE = identifier (e.g., BLOCK)
ALTER DATABASE SCOPED CONFIGURATION SET TXN_PRIORITY_MODE = BLOCK;

-- TXN_PRIORITY_MIN_WAIT_MILLISECONDS = integer
ALTER DATABASE SCOPED CONFIGURATION SET TXN_PRIORITY_MIN_WAIT_MILLISECONDS = 5000;

-- TXN_PRIORITY_MIN_WAIT_MILLISECONDS = 0
ALTER DATABASE SCOPED CONFIGURATION SET TXN_PRIORITY_MIN_WAIT_MILLISECONDS = 0;

-- FOR SECONDARY with TXN_PRIORITY_MODE
ALTER DATABASE SCOPED CONFIGURATION FOR SECONDARY SET TXN_PRIORITY_MODE = PRIMARY;

-- FOR SECONDARY with TXN_PRIORITY_MIN_WAIT_MILLISECONDS
ALTER DATABASE SCOPED CONFIGURATION FOR SECONDARY SET TXN_PRIORITY_MIN_WAIT_MILLISECONDS = 1000;
