SELECT id, COUNT(f_col1) OVER () FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col1) OVER ( ORDER BY shuffled_id) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col1) OVER ( ORDER BY shuffled_id  ROWS 1 PRECEDING) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col1) OVER ( ORDER BY shuffled_id  ROWS 1 PRECEDING) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col4) OVER ( ORDER BY id  ROWS BETWEEN 1 FOLLOWING  AND 1 FOLLOWING ) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col1) OVER ( ORDER BY shuffled_id  RANGE BETWEEN UNBOUNDED PRECEDING  AND CURRENT ROW ) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col3) OVER ( ORDER BY shuffled_id DESC  RANGE BETWEEN CURRENT ROW  AND UNBOUNDED FOLLOWING ) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col3) OVER (PARTITION BY p_col1 ORDER BY o_col1, unique_id, o_col3 DESC ROWS BETWEEN CURRENT ROW  AND CURRENT ROW ) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col3) OVER ( ORDER BY shuffled_id  RANGE BETWEEN CURRENT ROW  AND UNBOUNDED FOLLOWING ) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col2) OVER (PARTITION BY p_col1 ORDER BY o_col2, o_col1, unique_id DESC RANGE BETWEEN CURRENT ROW  AND CURRENT ROW ) FROM table_003 ORDER BY id;

SELECT id, COUNT(f_col1) OVER (PARTITION BY p_col3, p_col2, p_col1 ORDER BY unique_id  RANGE BETWEEN UNBOUNDED PRECEDING  AND CURRENT ROW ) FROM table_003 ORDER BY id;

SELECT id, PERCENTILE_CONT(0.00001)WITHIN GROUP (ORDER BY shuffled_id) OVER (PARTITION BY p_col2, p_col1, p_col3) FROM table_003 ORDER BY id;

SELECT id, PERCENTILE_DISC(0.21)WITHIN GROUP (ORDER BY unique_id) OVER () FROM table_003 ORDER BY id;

SELECT id, PERCENTILE_DISC(0.99999)WITHIN GROUP (ORDER BY shuffled_id) OVER (PARTITION BY p_col3, p_col2, p_col1) FROM table_003 ORDER BY id;