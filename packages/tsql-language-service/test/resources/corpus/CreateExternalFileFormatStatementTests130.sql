CREATE EXTERNAL FILE FORMAT eff1
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT
    );

CREATE EXTERNAL FILE FORMAT eff2
    WITH (
    FORMAT_TYPE = RCFILE,
    SERDE_METHOD = 'org.apache.hadoop.hive.serde2.columnar.ColumnarSerDe'
    );

CREATE EXTERNAL FILE FORMAT eff3
    WITH (
    FORMAT_TYPE = ORC
    );

CREATE EXTERNAL FILE FORMAT eff4
    WITH (
    FORMAT_TYPE = PARQUET
    );

CREATE EXTERNAL FILE FORMAT eff5
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (FIELD_TERMINATOR = '|', STRING_DELIMITER = ';', DATE_FORMAT = 'MM-dd-yyyy', USE_TYPE_DEFAULT = FALSE)
    );

CREATE EXTERNAL FILE FORMAT eff6
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.GzipCodec'
    );

CREATE EXTERNAL FILE FORMAT eff7
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (FIELD_TERMINATOR = '|', STRING_DELIMITER = ';', DATE_FORMAT = 'MM-dd-yyyy', USE_TYPE_DEFAULT = FALSE),
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.GzipCodec'
    );

CREATE EXTERNAL FILE FORMAT eff8
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.GzipCodec',
    FORMAT_OPTIONS (FIELD_TERMINATOR = '|', STRING_DELIMITER = ';', DATE_FORMAT = 'MM-dd-yyyy', USE_TYPE_DEFAULT = FALSE)
    );

CREATE EXTERNAL FILE FORMAT eff9
    WITH (
    FORMAT_TYPE = RCFILE,
    SERDE_METHOD = 'org.apache.hadoop.hive.serde2.columnar.ColumnarSerDe',
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.DefaultCodec'
    );

CREATE EXTERNAL FILE FORMAT eff10
    WITH (
    FORMAT_TYPE = RCFILE,
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.DefaultCodec',
    SERDE_METHOD = 'org.apache.hadoop.hive.serde2.columnar.ColumnarSerDe'
    );

CREATE EXTERNAL FILE FORMAT eff11
    WITH (
    FORMAT_TYPE = ORC,
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.DefaultCodec'
    );

CREATE EXTERNAL FILE FORMAT eff12
    WITH (
    FORMAT_TYPE = PARQUET,
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.GzipCodec'
    );

CREATE EXTERNAL FILE FORMAT eff13
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (FIELD_TERMINATOR = '|')
    );

CREATE EXTERNAL FILE FORMAT eff14
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (STRING_DELIMITER = ';')
    );

CREATE EXTERNAL FILE FORMAT eff15
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (DATE_FORMAT = 'MM-dd-yyyy')
    );

CREATE EXTERNAL FILE FORMAT eff16
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (USE_TYPE_DEFAULT = TRUE)
    );

CREATE EXTERNAL FILE FORMAT eff17
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (FIELD_TERMINATOR = '|', STRING_DELIMITER = ';')
    );

CREATE EXTERNAL FILE FORMAT eff18
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (DATE_FORMAT = 'MM-dd-yyyy', FIELD_TERMINATOR = '|')
    );

CREATE EXTERNAL FILE FORMAT eff19
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (ENCODING = 'UTF8')    
    );

CREATE EXTERNAL FILE FORMAT eff20
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (ENCODING = 'UTF16')
    );

CREATE EXTERNAL FILE FORMAT eff21
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (FIRST_ROW = 1)
    );

CREATE EXTERNAL FILE FORMAT eff22
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (FIRST_ROW = 5)
    );

CREATE EXTERNAL FILE FORMAT eff23
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    FORMAT_OPTIONS (ENCODING = 'UTF8', FIRST_ROW = 1)
    );

CREATE EXTERNAL FILE FORMAT eff24
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.GzipCodec',
    FORMAT_OPTIONS (FIRST_ROW = 1, ENCODING = 'UTF8')
    );

CREATE EXTERNAL FILE FORMAT eff25
    WITH (
    FORMAT_TYPE = DELIMITEDTEXT,
    DATA_COMPRESSION = 'org.apache.hadoop.io.compress.GzipCodec',
    FORMAT_OPTIONS (FIRST_ROW = 1, FIELD_TERMINATOR = '|', ENCODING = 'UTF8', STRING_DELIMITER = ';', DATE_FORMAT = 'MM/dd/yyyy')
    );