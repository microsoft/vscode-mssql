CREATE SERVER AUDIT a1 TO FILE (FILEPATH='aaa')

CREATE SERVER AUDIT a1 TO FILE (FILEPATH = 'aaa', MAXSIZE = 10 MB, MAX_ROLLOVER_FILES = 1, RESERVE_DISK_SPACE = ON) 
	WITH (QUEUE_DELAY = 10)

CREATE SERVER AUDIT a1 TO SECURITY_LOG WITH (ON_FAILURE = CONTINUE, ON_FAILURE = SHUTDOWN, AUDIT_GUID='ca761232-ed42-11ce-bacd-00aa0057b223')
GO

ALTER SERVER AUDIT a1 TO FILE (MAXSIZE = 5 GB)

ALTER SERVER AUDIT a1 TO FILE (MAXSIZE = 15 TB, MAXSIZE = UNLIMITED, 
		FILEPATH = 'ccc', MAX_ROLLOVER_FILES = unlimited, RESERVE_DISK_SPACE = ON)

ALTER SERVER AUDIT a1 TO APPLICATION_LOG WITH (QUEUE_DELAY = 30)

ALTER SERVER AUDIT a1 WITH (STATE = OFF, ON_FAILURE = SHUTDOWN, ON_FAILURE = CONTINUE)

ALTER SERVER AUDIT a1 MODIFY NAME = a2
GO

DROP SERVER AUDIT a1