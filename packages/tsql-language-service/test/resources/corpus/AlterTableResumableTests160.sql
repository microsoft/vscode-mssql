-- ALTER TABLE ADD CONSTRAINT with RESUMABLE option (SQL Server 2022+)
ALTER TABLE table1 ADD CONSTRAINT PK_Constrain PRIMARY KEY CLUSTERED (a) WITH (ONLINE = ON, MAXDOP = 2, RESUMABLE = ON, MAX_DURATION = 240);
ALTER TABLE table1 ADD CONSTRAINT PK_Constrain PRIMARY KEY CLUSTERED (a) WITH (RESUMABLE = ON);
ALTER TABLE table1 ADD CONSTRAINT PK_Constrain PRIMARY KEY CLUSTERED (a) WITH (RESUMABLE = OFF);
ALTER TABLE table1 ADD CONSTRAINT UQ_Constrain UNIQUE (b) WITH (RESUMABLE = ON, MAX_DURATION = 120 MINUTES);
