{
    "name": "@mssqlperf/contracts",
    "version": "0.1.0",
    "private": true,
    "description": "Shared perf contracts: marker/result/config schemas, TS types, runtime validators",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist",
        "schemas",
        "sql"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test": "vitest run"
    },
    "dependencies": {
        "ajv": "^8.17.1"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "jsonc-parser": "^3.3.1",
        "typescript": "^5.8.0",
        "vitest": "^3.2.0"
    }
}
