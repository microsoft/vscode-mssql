{
    "name": "@mssqlperf/observability-contracts",
    "version": "0.1.0",
    "private": true,
    "description": "Shared Observability Contract: event vocabulary registry, field classifications, timing classes, metric eligibility",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "files": [
        "dist",
        "generated"
    ],
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "generate": "node dist/generate.js",
        "test": "vitest run"
    },
    "devDependencies": {
        "@types/node": "^24.0.0",
        "typescript": "^5.8.0",
        "vitest": "^3.2.0"
    }
}
