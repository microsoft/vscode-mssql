# .NET build output from the sample .sqlproj fixtures. These are regenerated by
# `dotnet build` (see dacpac/README.md) and must not be committed. The pre-built
# .dacpac fixtures the harness references live in dacpac/, which is tracked.
sqlproj/bin/
sqlproj/obj/
sqlproj-clean/bin/
sqlproj-clean/obj/
